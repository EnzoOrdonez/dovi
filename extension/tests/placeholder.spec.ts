import { describe, expect, it } from "vitest";

// Placeholder — asegura que vitest arranca en verde antes de añadir specs reales.
describe("dovi extension smoke", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
